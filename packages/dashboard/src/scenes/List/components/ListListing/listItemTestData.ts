export type itemDataTypes = {
  id: number;
  task: string;
  schedule?: string;
  color?: string;
  subTasks: number;
};

const listItemTestData: itemDataTypes[] = [];

export default listItemTestData;
