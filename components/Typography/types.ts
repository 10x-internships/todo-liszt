export type TypographyProps = {
  size?: TypographySizes;
};

export enum FontWeight {
  REGULAR = 400,
  MEDIUM = 500,
  SEMI_BOLD = 600,
  BOLD = 700,
}

export enum TypographySizes {
  SM = 'sm', // small
  MD = 'md', // medium
  LG = 'lg', // large
  XL = 'xl', // extra-large
}
